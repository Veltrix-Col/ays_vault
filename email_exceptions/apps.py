from django.apps import AppConfig

class EmailExceptionsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "email_exceptions"
    verbose_name = "Excepciones de Correo"
