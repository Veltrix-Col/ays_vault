document.addEventListener('DOMContentLoaded',()=>{let activeField=null,timer=null;const panel=document.querySelector('#reveal-panel'),form=document.querySelector('#reveal-form');document.querySelectorAll('.reveal-btn').forEach(b=>b.addEventListener('click',()=>{activeField=b.dataset.field;document.querySelector('#field-input').value=activeField;panel.hidden=false;panel.scrollIntoView({behavior:'smooth'});}));if(form)form.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form),csrf=form.querySelector('[name=csrfmiddlewaretoken]').value;const r=await fetch(form.action,{method:'POST',body:data,headers:{'X-CSRFToken':csrf}});const j=await r.json();if(!r.ok){alert('No fue posible confirmar. Verifique contraseña, motivo y permisos.');return;}const target=document.querySelector('#value-'+activeField);target.textContent=j.value;target.classList.add('revealed');const copy=document.createElement('button');copy.className='btn secondary';copy.textContent='Copiar';copy.onclick=async()=>{await navigator.clipboard.writeText(j.value);const m=document.querySelector('#copy-meta');const fd=new FormData();fd.append('field',activeField);fd.append('reason',data.get('reason'));await fetch(m.dataset.url,{method:'POST',body:fd,headers:{'X-CSRFToken':csrf}});copy.textContent='Copiado y auditado';};target.parentElement.appendChild(copy);clearTimeout(timer);timer=setTimeout(()=>{target.textContent=activeField==='pan'?'•••• •••• ••••':'••/••';copy.remove();panel.hidden=true;form.reset();},25000);});});
