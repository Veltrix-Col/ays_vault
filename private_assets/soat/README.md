# Referencia operativa SOAT

Ubique aquí `SOAT_prueba_3_Def.xlsx` o configure `SOAT_REFERENCE_FILE` con una ruta privada equivalente.

El archivo `.xlsx` está excluido de Git y no debe copiarse a `static/`, `media/` ni una ruta pública.
